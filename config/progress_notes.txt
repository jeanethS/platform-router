# progress_notes.txt
# Session: 2026-07-12

## Session init
- Read tasks.json + progress_notes.txt.
- Reviewed BullMQ migration diff d3408a9..3e4c138.
- Verified test suite: 51 passed, 8 suites, tsc clean.

## What was done this session
- Updated tasks.json:
  - Added T-016 "Migrate Kafka connector to BullMQ" -> complete.
  - Added T-017 "Fix Jest open handles from ConfigService fs.watch" -> complete.
- Fixed open handles (T-017):
  - Root cause: ConfigService._watch() created fs.watch FSWatchers that were never closed in tests.
  - Added _watchers: fs.FSWatcher[] to ConfigService.
  - Added ConfigService.close() to close all watchers and clear listeners.
  - Wired fs.watch() return values into _watchers.
  - Added tests/config.test.ts afterEach hook to close ConfigService.instance.
  - Updated tests/integration.test.ts to close its ConfigService in afterAll.
  - Added new test "close() stops file watchers from firing callbacks" (TDD red -> green).

## Final state
- 52/52 tests passing, 8 suites.
- tsc --noEmit clean.
- `npx jest --runInBand --detectOpenHandles` reports zero open handles.
- Console noise remains: expected console.warn from config hot-reload invalidation tests and priority neutral fallback tests.
- Linter: `npm run lint` fails due to missing `typescript-eslint` package in eslint.config.js; pre-existing environment issue, not project code.

## Files changed this session
- config/tasks.json
- config/progress_notes.txt
- src/config.ts
- tests/config.test.ts
- tests/integration.test.ts
