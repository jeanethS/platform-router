apiVersion: v1
kind: Service
metadata:
  name: platform-router
  labels:
    app: platform-router
spec:
  selector:
    app: platform-router
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 9090
      targetPort: 9090
      name: metrics
